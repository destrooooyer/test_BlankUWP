<Page
    x:Class="App1.settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Click="Button_Click_1" Style="{StaticResource SplitViewTogglePaneButtonStyle}"></Button>
        <TextBlock Text="设置"
                   Style="{ThemeResource TitleTextBlockStyle}" 
                   VerticalAlignment="Center"
                   Grid.Column="1"/>


        <ListView
            Name="sp"
            Grid.Row="1" Grid.ColumnSpan="2">
            <Border BorderBrush="WhiteSmoke" BorderThickness="1">
                <Button Height="100" Width="{Binding Path=ActualWidth,ElementName=sp}"
                    Click="Button_Click"
                    Name="chooseXls">
                    未选择课表，请选择课表
                </Button>
            </Border>

            <CalendarDatePicker x:Name="date" Height="37"  Width="{Binding Path=ActualWidth,ElementName=sp}"/>
            <Border BorderBrush="WhiteSmoke" BorderThickness="1">
                <Button Height="100" Width="{Binding Path=ActualWidth,ElementName=sp}" Click="Button_Click_2">
                        提交
                </Button>
            </Border>
        </ListView>


        <local:pane x:Name="pane" 
                    Grid.ColumnSpan="2"
                    Grid.RowSpan="2" d:IsHidden="True"></local:pane>

    </Grid>
</Page>
